<% form_for([:admin, @contact]) do |f|%>
  <%= f.error_messages %>
  <p>
    <%= f.label :Контакт %><br />
    <%= f.text_area :contact, :cols => '70', :class => "mceEditor" %>
  </p>
  <%= f.submit "Создать" %>
<%end%>
<%= link_to "Назад", admin_contacts_path %>